- выдача слов для повторения, количество слов за раз берется из конфига, поторенные слова учитываются,
	количество повторений так же берется из конфига. Так же в зависимости от времени последнего повторения, меняется
	вероятность попадания слова в список на повторение. Нужно учитывать сложность слова (N5, N4 и т.д.) - слова
	более простые будут получать большую вероятность на попадение, чем слова с большей сложностью.

- проверка встречаемости отдельных символов из переданной строки в словаре. Если символы встречаются достаточно
	часто, тогда их надо добавить в список символов для изучения
- аналогично со словами (если нужно искать слово, то можно передать флаг)
- степень всречаемости, по достижении которой нужно добавить слово в список можно регулировать в конфиге
- можно сделать чтение из буфера (файла) сразу несколько подобных строк, вместо передачи по одной строке
- сделать аналог anki в командной строке
- сделать возможность добавить файл из которого надо искать встречаемость
- добавить возможность сравнивать список составленных неизвестных слов со список N5, N4 и тд, и вычислять, насколько сильно
	эталонный список пересекается, т.е. насколько сильно пользователь не знает канджи на этот уровень
- возможность добавлять канджи (слово), его чтения (может быть несколько), и толкования (может быть несколько)
- соответственно возможность доставать канджи по чтению (поиск) или по канджи доставать его чтение или перевод. Таким образом,
	используя это приложение, накапливается база знаний конкретного пользователя
- добавить возможность синхронизации через какой либо сервис (гугл драйв, яндекс диск и тд)
- по встречаемости смотреть, если канджи (только; слова не считаются, хотя можно сделать вхождения слова в более длинное)
	встречается достаточно часто, то этот канджи добавлять в список для отработки (возможно вместе с канджи подтягивать и все слова
	из списка, в которых он встречается)
- отработка канджи из списка (возможно считать более приоритетными те слова и канджи, которые встречаются в списках N5, N4...;
	причем чем проще уровень, тем более приоритетно добавить именно этот канджи). Список для отработки за раз ограничен
	предельным значением из конфига
- при отработке запускается интерактивная сессия, во время которой надо либо по переводу определить слово (написать в виде канджи,
	либо в виде чтения), либо, наоборот, по канджи или чтению угадать в обратную сторону. Надо сделать так, чтобы пользователь не
	точь в точь угадывал, как это записано в списке, а с некоторой погрешностью (возможно опечатки в написании перевода, или
	перепутал один символ, но все равно приложение должно понять, что это и засчитать). Возможно сразу несолько вариантов ответа.
	Оценка правильности состоит из 2-х параметров: например, если дано слово в виде канджи, надо написать его чтение (хотя бы одно),
	и перевод (хотя бы один). В таком случае вопрос считается отвеченным.
- вести список известных канджи и слов
- из переданного текста или изображения считать все неизвестные канджи и добавить в список на изучение
- ocr


- перевести проект из консольного приложения в веб сервис

done:
	- добавлять слова в словарь, учитывать, что слово повторилось (например, счетчик)

